{{#if this.entity}}
  {{yield (hash
    world=@w.world
    Entity=(component "ecsy/entity" createEntity=@createEntity parent=this)
    private=(hash
      componentReference=this
      entity=this.entity
    )
  )}}
{{/if}}