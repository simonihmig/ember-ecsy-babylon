{{#if this.entity}}
  {{yield (hash
    world=@w.world
    Entity=(component "ecsy/entity" createEntity=@createEntity parent=this)
    LoadGltf=@world.LoadGltf
    private=(hash
      componentReference=this
      entity=this.entity
    )
  )}}
{{/if}}