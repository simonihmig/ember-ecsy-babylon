{{#if this.assets}}
  {{yield
    this.assets
    (hash
      world=@w.world
      Entity=@w.Entity
      LoadGltf=@w.LoadGltf
      LoadGltfs=@w.LoadGltfs
      private=(hash
        componentReference=this
        createEntity=@w.private.createEntity
      )
    )
  }}
{{/if}}